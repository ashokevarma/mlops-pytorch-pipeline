# Inference-only dependencies (no training extras such as tensorboard).
# Kept deliberately lean to keep the serving image small.
torch==2.3.1
torchvision==0.18.1
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
Pillow==10.3.0
python-multipart==0.0.9
